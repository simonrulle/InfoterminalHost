<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="InfoterminalHost.Views.TimetablesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:InfoterminalHost.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:model="using:InfoterminalHost.Models" 
    mc:Ignorable="d"
    >

    <Grid>
        <ListView ItemsSource="{x:Bind ViewModel.courses}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Expander Header="{Binding Name}">
                        <ListView ItemsSource="{Binding Semester}">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding Name}"
                                        Click="SemesterButton_Click"
                                        />
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Expander>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>

</Page>
